import { styled } from '../styled';
import { FullHeight } from './FullHeight';

export const AppRoot = styled(FullHeight, 'AppRoot')`
  position: relative;
  overflow: hidden;
  margin: 0;
  padding: 0;
  border: none;
`;
